# EasyRAG RAG 服务（模块 B：索引管线）依赖
#
# 只列直接依赖并锁定版本；传递依赖交给 pip 解析。
# Python 3.14 实测可用（chromadb 1.5.9 有 cp314 wheel）。

# --- Web 框架 ---
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32

# --- 资料真相与迁移（knowledge 模块独占）---
SQLAlchemy==2.0.52
PyMySQL[rsa]==1.2.0
alembic==1.20.0

# --- 配置 ---
pydantic==2.13.5
pydantic-settings==2.15.0

# --- 向量索引 ---
# 选 Chroma 不选 FAISS（B-3）：原生元数据过滤与按 document_id 删除、
# 持久化开箱即用。几百篇规模下 FAISS 的性能优势无意义，而元数据能力缺失是实痛。
chromadb==1.5.9
tokenizers==0.23.2
# 默认 tokenizer 缺失时按固定版本下载
huggingface_hub==1.31.0

# --- 词法索引（M2 建好，M4 混合检索启用）---
rank-bm25==0.2.2
jieba==0.42.1

# --- HTTP 客户端（探测 embedding 端点）---
httpx==0.28.1

langchain==1.3.17
langchain-core==1.6.0
langchain-openai==1.6.0
langchain-deepseek==1.1.0
openai==3.8.0
httpx2==2.12.0

# --- 与前端 Markdown 语义一致的正文引用校验 ---
markdown-it-py==4.2.0

# --- 测试 ---
pytest==9.1.1
