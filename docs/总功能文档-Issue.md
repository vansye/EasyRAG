# 总功能文档：EasyRAG 个人知识库问答系统

> 2026-09-16 交付：前置修复、问答历史、出处布局与 M4 第一项的 PR #59–#64 已全部合入 main，主分支 CI 通过。按[分阶段设计](m4-retrieval-and-history-plan-2026-09-15.md)，优先级 2 通过 [PR #65](https://github.com/vansye/EasyRAG/pull/65) 实施启动阶段的本地 SDK 准备，不要求模型配置或发送预热请求；后续为流式和检索对照。回答约束、计时及 SDK 准备不等同于召回率提升。PR、CI、文件边界及验收以[本轮记录](m4-progress-2026-09-16.md)为准。

> 本 Issue 是功能总纲：定义产品边界与模块划分。每个模块的详细设计（功能描述、数据原型伪代码、对外接口伪代码、模块边界说明）在子 Issue 中展开，挂在本 Issue 下。

> 2026-09-15 架构迁移：采用单 FastAPI 后端和独立业务模块，详见 [模块设计与实施检查点](fastapi-modules.md)。现有 Vue 工作台和回答模型配置已完成于 PR #34；迁移继承其行为。下文尚未实现的 URL 收录、检索改写和评估界面仍属后续产品规划，本次不随技术迁移扩展。

## 一、背景

收藏 ≠ 拥有：资料越攒越多、散落各处，无法检索与调用。本项目做一个能管理和调用知识的系统——随手丢进去，用一句话问出来，答案附出处。

## 二、用户画像

目标用户：**有持续记笔记、存资料习惯的个人**——学生、开发者、研究者。

| 维度 | 基线假设（必须支持） | 增强场景（有则更好） |
|---|---|---|
| 资料形态 | 普通 markdown 笔记、纯文本、网页链接 | 带 frontmatter 的结构化笔记 |
| 组织方式 | 正文有标题层级，目录结构随意 | 目录分类清晰、已有标签体系 |
| 使用工具 | 任意编辑器 | Obsidian / Typora / VS Code |
| 规模 | 几十到几百篇 | —— |

**明确不假设**：不假设用户使用 Obsidian、不假设写 frontmatter、不假设文件命名与目录规整。核心链路只依赖普遍存在的结构信号（文档标题、Markdown 标题层级）；frontmatter 的 `tags` 属于可选增强，缺失时功能完整、只是少一个过滤维度。

**不为谁设计**：团队与企业的多人协作场景（无权限体系）、以 PDF / Word 文献库为主的用户（不做富格式解析）、需要在系统内写作的用户（不做编辑器）。

## 三、产品定义

一个网页，三件事：

1. **收录与管理资料**：md / txt / 链接，支持更新与删除
2. **问答**：一句话提问 → 答案 + 可点开的出处（定位到文档与位置）
3. **更新同步**：资料更新或删除后，问答结果随之更新

拒答是一等行为：检索质量不足时返回"库里没有"，不进入生成。

## 四、功能边界

### 做（范围内）

- 资料收录（md / txt / URL）、列表、更新、删除
- 切片、embedding、检索、agent 多步检索
- 带出处的问答、拒答
- 黄金问答集与评估（hit@k / 拒答正确率）
- 检索过程展示（重查次数、采用/丢弃的片段）
- 独立问答历史：持久保存回答与当时出处、分页查看、删除、按当前资料重新提问；不自动作为后续上下文
- 流式输出：按 M4 后续步骤验证首字与完整耗时，完成后校验引用并保存历史

### 不做（范围外）

| 项 | 理由 |
|---|---|
| 资料编辑器 / 阅读器 | 编辑发生在本地工具，系统只收录与管理 |
| 登录、多用户、权限 | 单用户工具 |
| PDF / Word 富格式解析 | 主知识形态为 md 与网页；解析工程量大、不增加检索深度（后续可作扩展） |
| Obsidian 插件形态 | 网页形态覆盖不依赖 Obsidian 的用户；RAG 服务独立于 UI 形态，插件可作为后续第二客户端接入同一套 API |

### 已定（原待定项的裁决）

- **收录入口形态：网页上传优先**。理由：网页形态覆盖面广（不依赖用户使用 Obsidian）；后续可向 Obsidian 插件方向演化——RAG 服务独立于 UI 形态，插件可作为第二客户端接入同一套 API。文件夹同步（以文件夹为真相源）暂不做，数据模型以 `source` 字段预留，实施遇阻或需要贴近本地知识库使用习惯时再启用。

## 五、模块划分（各对应一个子 Issue）

| 模块 | 位置 | 职责 |
|---|---|---|
| **A 资料管理** | Python knowledge | 资料/切片、输入规则、MySQL 短事务与文档状态；唯一业务表写入者 |
| **B 检索索引** | Python retrieval | 切片、embedding、Chroma 维护与检索；不访问 MySQL |
| **C 问答 Agent** | Python qa | 使用注入的检索/模型接口完成判断、生成、拒答和 trace；不访问存储 |
| **D 评估** | 离线 CLI | 维护现有黄金集、跑集、指标和报告；不进入线上依赖链 |
| **E 前端** | Vue | 收录/管理界面、问答界面（答案 + 出处 + 检索过程） |
| **F 回答模型** | Python answer_models | 配置文件、密钥边界、地址解析和模型会话 |
| **G 应用接入** | FastAPI application | HTTP、用例编排、门禁、单执行者、恢复、健康检查与 CLI |

模块间通信：**前端 ↔ FastAPI（REST）**；G 通过 A/B/C/F 的公开 Python 接口调用。四个业务模块互不引用，C 的检索和模型接口由 G 注入；不传递数据库连接、索引对象或 SDK 客户端。

写入流由 G 安排：A 保存原文 → B 切片 → A 落库生成 chunk ID → B embedding 与索引替换 → A 确认终态。G 持有跨步骤的变更许可；A/B 分别负责自身事务和清理。

## 六、用户故事

| # | 用户故事 | 涉及模块 |
|---|---|---|
| U1 | 我把一篇资料丢进系统，它出现在列表里 | A、E |
| U2 | 我删掉一篇资料，后续新问答不再引用它；已有历史保留当时快照 | A、B、C |
| U3 | 我改了资料正文，再问同一个问题，答案基于新内容 | A、B、C、E |
| U4 | 我问一句话，得到答案，结论旁有可点开的出处 | C、A、E |
| U5 | 我问一个知识库里没有的问题，系统说"库里没有" | C |
| U6 | 我的问题一次检索查不到（同义词），系统改写后重查并答出 | C |
| U7 | 我能看到这次问答检索了哪些片段、丢弃了哪些、重查了几轮 | C、E |
| U8 | 我跑一遍黄金问答集，看到 hit@k 与拒答正确率 | D、E |
| U9 | 我查看曾经的回答与当时出处，或按当前资料重新提问 | A、C、E、F、G |

## 七、架构不变量

1. **MySQL 是资料、切片与已完成问答历史的唯一真相源**，且只有 A 写入。模型配置由 F 管理本机文件；现有黄金集和离线评估报告由 D 管理文件。问答 trace 随响应返回，并与当时来源一起保存在历史快照中；技术失败不保存为已完成记录。
2. **派生索引可重建**：向量与词法索引按 chunk_id 关联 MySQL，丢失即重算，不构成事故。
3. **溯源链路贯穿全程**：chunk_id 从索引带到 API 响应，中途不丢。
4. **拒答是一等行为**：检索质量不足时不进入生成。

## 八、设计文档必答题 → 落点

| 必答题 | 落点 |
|---|---|
| 知识怎么建模和组织？为什么利于检索？ | 模块 A/B 子 Issue + 设计文档 |
| 检索链路怎么设计？不准时怎么办？ | 模块 C 子 Issue + 评估数字 |
| agent/工作流怎么处理"一步答不好"？ | 模块 C 的循环设计（改写 → 重查 → 限度 → 拒答） |
| 怎么评估检索质量？ | 模块 D：黄金集、hit@k、拒答正确率 |

## 九、里程碑

| 阶段 | 内容 | 验收 |
|---|---|---|
| M0 | 样例语料（29 篇无隐私笔记：17 篇黄金集出处 + 12 篇同主题干扰项）+ 黄金问答集 30 题（直答 15 / 库外 8 / 需改写 7） | 集合入库 |
| M1 | 三端骨架 | 三进程可起、健康检查通、CI 绿 |
| M2 | 收录 + 索引 + 朴素问答 | U1、U4 跑通 |
| M3 | 更新同步 + 拒答 | U2、U3、U5 有测试 |
| M4 | 回答约束与计时 → 首问/流式延迟 → 基于失败样例的检索改进（每项一个 PR） | 分别验收规则、首字/完整耗时；检索改动报告 hit@k、MRR、回答/拒答与引用回归 |
| M5 | 评估功能 + 检索过程展示 | U7、U8 跑通 |

M0 先于代码：黄金集是检索结论的度量衡。

## 十、协作规则

- 一个功能一个 PR，关联本 Issue 与对应子 Issue；描述说明做了什么、为什么。
- 检索类改进附前后评估数字。
- commit 有意义，粒度小，不混入无关改动。

## FastAPI 迁移交付（2026-09-15）

统一后端迁移、模块回归与本机切换已完成，交付入口为 [PR #56](https://github.com/vansye/EasyRAG/pull/56)。A/B/C/F 互不引用，G 只调用公开入口；各模块维护自己的类型、异常、存储和测试。Vue 外观、模型配置与业务接口保留，本次未增加轮询。

| 模块 | 子 Issue | 功能 PR |
|---|---|---|
| A 资料管理 | #2 | #40、#42、#43、#49 |
| B 检索索引 | #3 | #41、#48、#54 |
| C 问答 | #27 | #46 |
| D 离线评估 | #37 | #47 |
| E 前端 | #33 | #34；迁移兼容验收见 #56 |
| F 回答模型 | #35 | #44 |
| G 应用编排 | #36 | #39、#45、#50、#51、#52、#53、#55、#56 |

设计 PR #38 在实现之前发布；以上子 Issue 均已建立父子关系，PR 按功能依次叠加，全部保留待审查。历史 A 增量 #15 与前端 PR #34 保留，没有自动合并或提前关闭整个模块 Issue。

验证：Linux CI 的 Python 默认 471 项、真实 MySQL 61 项、Vue 15 项与构建通过。真实浏览器完成模型配置、资料上传/修改/重处理/删除、引用和拒答。备份恢复及索引重建保留 13 份原资料和 623 个切片 ID，具体过程见 [切换记录](fastapi-cutover.md)；接口见 [API 文档](api.md)。

以上为迁移时的验收记录；URL 收录、BM25、改写重查和完整评估平台仍未交付。问答历史及 M4 的最新实施状态见本文顶部的本轮记录。
