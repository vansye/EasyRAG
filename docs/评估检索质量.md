# 怎么评估检索质量（设计文档必答题）

> 2026-09-23 整理。本文是必答题"怎么评估检索质量"的正文，把 M0 的黄金集、M4 优先级 4/5 的三轮评估（PR #76 → #77 → #78 → #79）串成一条线：**先定度量衡，再让数字决定做什么、不做什么**。原始报告在 `eval/`，指标口径的逐条裁决在 [子 Issue D](子Issue-D-离线评估.md)，检索与判定的实现取舍在 [子 Issue B](子Issue-B-索引管线.md) B-17 与 [子 Issue C](子Issue-C-问答Agent.md) C-6。

## 一、一句话回答

检索质量不是一个数，是三个问题，各自要一把尺子：

| 问题 | 尺子 | 调不调回答模型 | 抓得住的失败 | 抓不住的失败 |
|---|---|---|---|---|
| 正确的那篇资料进候选了吗？ | 离线召回 hit@k（k = 1/3/5/10） | 不调 | 漏召回、被同主题专篇压出前几名 | 判定错、引用错篇、越出片段 |
| 系统答对还是拒对了？引用的是那一篇吗？ | 在线评估：拒答正确率、误拒率、部分覆盖冒充完整、**错源率** | 调 | 库外题没拒、库内题误拒、答对但引用错篇 | 句子级越界 |
| 答案有没有越出片段？ | 逐句引用支持率（同一模型做判官） | 调，约翻倍 | 缩写展开、推理补充、改写 | 判官自身的假阴性（所以读作下界） |

再加一把与质量无关但用户直接感受的尺子：端到端延迟——首段文本与完整耗时的中位数/最大值，它是"检索效率"的唯一口径，hit@k 不是。

三把尺子分层不是为了指标好看：每一层都有上一层测不出的失败，而 M4 三轮的每一次改动都是被某一层的具体失败题驱动的。

## 二、度量衡先于代码：黄金集怎么出

**M0 先写题再写代码。** 没有题集，任何"检索变好了"都只是感觉。题集设计有四个取舍：

1. **四类题各对一个评审预告的追问。** 直答类 Q（答案在库内，标注唯一出处）、库外类 N（必须拒答，回答"知识库里没有的内容系统会怎样"）、部分覆盖类 P（有直接相关片段但主体缺失，须基于片段作答并声明边界）、需改写类 R（原词大概率查不到）。P 类是实测后单立的：相似度阈值分不开直答与部分覆盖（Q1 = 0.579，P1 = 0.588 反而更高），子方面覆盖数差距太小且有词面假高分，所以"这段文字够不够回答这个问题"只能由模型判——这是三态判定器存在的理由，也是评估口径里 P 类单独计分的理由。
2. **只标唯一出处，同主题干扰命中记 miss。** 测的是"找到最佳答案"，不是"找到相关内容"。语料 29 篇里 12 篇是同主题干扰项，专门用来让"找错篇"暴露出来。
3. **N/P 类只展示召回，不进 hit@k 分母。** 离线脚本不调模型，而拒答只有模型能判；用召回结果冒充拒答判定比不测更糟（D-1）。
4. **三集分开报告，不合并。** v1（30 题）是回归集，冻结不改；v2（12 题，2026-09-13）是留出集；v3（19 题，2026-09-22）是难例集。每集有自己的来源和目的：

| 集 | 题数 | 为什么要它 | 出题来源 |
|---|---|---|---|
| v1 回归集 | Q16 / N7 / P1 / R6 | M0 度量衡；M2 基线 | 语料的 17 篇出处文档 |
| v2 留出集 | Q6 / R2 / N3 / P1 | v1 基线 hit@5 = 22/22 饱和，任何改进只能写"100%→100%"；且 12 篇干扰项从未作为正确答案考过（"能测能不能找到，测不出会不会找错"） | 干扰项作正确出处；N 类改为"主题在库、内容缺失"的模糊边界题 |
| v3 难例集 | Q12 / R3 / P4，每题标难点类型 | 混合检索后 v1/v2 再次饱和；真实使用记录里的失败问法和黄金集的"端正问法"长得不一样 | `scripts/export_history_failures.py` 只读导出本机历史里 REFUSED / PARTIAL 的 10 条（错别字 `checkpiont`、缩写 `FDE`、小写 `git`），人工标注后据此出题；真实问法不入库 |

**过拟合治理先治度量衡。** 语料全部来自作者自己的笔记（三个技术域、同一套模板），这个担忧是真的；但过拟合的症状是度量衡失去分辨力，而不是语料同质本身。所以第一刀切在 hit@k 曲线与留出集，管线一行不碰（判断力记录 #19）。v3 把"哪一类模糊匹配还不行"变成可分项统计的数字，而不是再加一批同款题。

## 三、在线评估补的三个指标，为什么离线不够

离线召回在 v1 上早已 22/22，但 2026-09-21 第一次在线跑（`eval/answers-baseline-2026-09-21.md`，纯稠密检索）暴露了一件离线永远看不到的事：

**Q21 答错了篇，而现有防线全部放行。** 问"文件写完返回成功、断电为什么还会丢、怎么强制落盘"，标注出处 OS Buffer 短卡逐字含答案措辞，却被 Redis 持久化/过期 Key 两篇专篇的 10 个片段压出前十。判定器看着 Redis 片段判 SUFFICIENT，答案引用 [1][2][3] 全是 Redis——引用格式合法、判定合法、内容也未编造，只是答的不是用户那篇。于是有了三条在线指标（D-1 到 D-3）：

- **错源率** = 已作答且有引用的 Q/R 里，引用片段全部不属于标注出处的比例。它区分两种成因——"标注出处没进候选"（检索层的问题）和"进了候选但没被引用"（生成层的问题），分别对应不同的改进点（D-2）。基线 1/30，成因是前者。
- **拒答正确率 / 误拒率 / P 类冒充完整**：N 类判 NONE 的比例、Q/R 判 NONE 的比例、P 类判 SUFFICIENT 的比例。基线 N 10/10、Q/R 误拒 0、P 1/2。
- **引用支持率**：同一回答模型逐句核对"是否完全由所引片段支持"，二值输出。**为什么用同一模型**：换更强的判官会把判官能力混进指标。**为什么逐句**：整段判定定位不到越界在哪。基线 86.9%（192/221），抽查不支持句分三类——缩写展开、推理性补充、接近改写的复述，没有编造事实。同一题两次运行句子级结果差异明显（R2 一次 1/3、一次 7/0），所以支持率只看总量趋势，不逐题比较（D-4）。

基线的判断：**幻觉的主要入口在检索层（错源），不在生成层（越界轻微且噪声大）**。这决定了 M4 后两步的顺序——先混合检索，再证据压缩。

## 四、三轮实测：数字怎么驱动改动

同一 42 题（v1 + v2）、同一语料、同一回答模型（agnes-3.0-flash）、同一 embedding（bge-m3），每轮只换一处。

### 第一轮：混合检索（B-17，PR #77）

靶子是 Q21。稠密向量按主题相近排序，精确措辞没有额外权重；BM25 恰好补这一点。

| 指标 | dense | hybrid（向量 + BM25 各取前 20，RRF k=60） |
|---|---|---|
| v1 hit@1 / hit@5 | 21/22 / 22/22 | **22/22** / 22/22（R1 补上） |
| v2 hit@1 / hit@5 | 7/8 / 7/8（Q21 前十之外） | 7/8 / **8/8**（Q21 第 4） |
| 在线：标注出处进候选 | 29/30 | **30/30** |
| 在线：错源率 | 1/30 | **0/30**——Q21 核心机制引用 OS Buffer 片段，Redis 相关部分引用 Redis 片段 |
| 拒答正确率 / 误拒 | 10/10 / 0 | 10/10 / 0 |
| Q/R 判 PARTIAL | 6 | 8（Q7、R6 由 SUFFICIENT 变 PARTIAL，各复验两次稳定） |
| 引用支持率 | 86.9% | 85.7%（噪声内） |
| 向量检索段中位 | 9 ms | 7 ms（BM25 与融合没有可测的延迟代价） |

三个取舍：**RRF 不做加权分数**，因为余弦与 BM25 量纲不同，加权系数没有理论依据，RRF 只用名次、无参数可调错；**bm25 单独不能作默认**，v1 退到 19/22@1；**Q7/R6 的代价不调权重去掩盖**——两题 top-1 仍是正确文档，答案只是多了覆盖边界声明，在 30 题上调系数就是过拟合。四种候选规则（dense / RRF / 末位补 1 / 末位补 2）离线对照过，只有 RRF 同时拿到 hit@1 29/30 与 hit@5 30/30。

### 第二轮：判定器标出相关片段，生成只看子集（C-6，PR #78）

靶子不是召回，是"送进生成的证据太多"：基线生成段占完整耗时八成以上，提示中位 1846 字符。判定器已经读过全部候选，让它在同一次调用里多输出"哪些片段支撑判断"，生成与引用只看这些片段、保留原编号。门槛写在前面：支持率不降、错源不升、格式失败率 < 2%、生成耗时与输入字符下降。

| 指标 | hybrid | hybrid + relevant |
|---|---|---|
| 送入生成的候选 | 160/160 | **76/160**（32 题全部只送子集，中位 2–3 个） |
| 标注文档的片段被判定器全部排除 | — | **0 题** |
| 生成提示字符 p25 / 中位 / p75 | 1617 / 1886 / 2234 | **775 / 1112 / 1311** |
| 生成调用 p25 / 中位 / p75 | 5588 / 7942 / 16562 ms | 2580 / 5213 / 9856 ms |
| 错源 / 拒答正确 / P 类 | 0/30 / 10/10 / 1/2 | 0/30 / 10/10 / 1/2 |
| 引用支持率 | 85.7% | 87.3%（噪声内） |
| 判定调用中位 | 978 ms | 956 ms（多输出一个数组没有可测代价） |

"标注文档片段被全部排除 = 0 题"是这一步唯一需要的安全检验：判定器挑出的子集没有丢掉回答所需的证据。提示字符数是代码可归因的确定性变化；生成耗时同向下降但第三方接口当日波动大，所以只写"中位与四分位同向下降"，不写百分比承诺。

### 第三轮：难例集 v3 与"不做改写重查"的证据（PR #79）

M4 计划的最后一步是"有界改写重查"，触发条件事先写明：**某集出现 hybrid 仍未进 top-5 的 Q/R 题**。v3 就是为这个条件准备的靶场——错别字、缩写、中英混用、精确术语、专篇压制短卡。

| 策略 | hit@1 | hit@5 | 未到第 1 的题 |
|---|---|---|---|
| dense | 13/15 | 15/15 | Q24（pdflush）第 2、Q31（Socket Buffer 短卡）第 3 |
| bm25 单独 | 14/15 | 15/15 | — |
| **hybrid（默认）** | **15/15** | **15/15** | — |

错别字题（`volumn`、`seesion`）两个通道都容错；两道新的"专篇压制短卡"在 hybrid 下都是第 1。在线（hybrid + relevant）：Q 12/12、R 3/3 无误拒，错源 0/15，判定器 19 题全部标出子集（95 个候选送入 39 个），首字中位 3.2 s、完整 9.9 s。

**结论：v1 / v2 / v3 三集在 hybrid 下都没有漏召回的题，改写重查没有可救的对象，C-2 维持不做。** 这个"不做"现在有三集数据撑着，比做了更站得住；钩子保留，触发条件不变。这也是对预告追问"为什么需要多步流程，一次检索 + 一次问答不行吗"的实测回答：在这套语料上一次混合检索够了，多步的必要性要由漏召回的题来证明，而不是预设。

## 五、被实测纠正的两处口径

评估口径不是一次定死的，两处被数据改过，都记在子 Issue D：

- **D-6：词法陷阱题从库外类改为部分覆盖类。** v3 最初把三道"术语在库、内容不在"的题（pdflush 被哪个版本替代、IndexedDB、epoll 在 Windows 怎么用）标为库外、期望拒答。结果三题全判 PARTIAL，答案明说"片段未提及、无法回答"并引用片段说明库里有什么，逐句核对无编造。对照三态定义——PARTIAL 是"主题直接相关、主体缺失"，NONE 是"主题不在"（v2 的 N8 被判 NONE 证明判定器分得清）——判定器是对的，标注错了。改标为 P4–P6，附加"不得编造缺失部分"的检验。**不改判定器把它们逼成拒答**，因为那会把 P1–P3 这类真实部分覆盖一起误杀，用户也失去"库里其实有相关内容"的提示。
- **D-7：元话语句不送核对。** "以上回答基于片段""覆盖边界说明"这类句子不含事实断言，判官却判为"不支持"。现在单独计数不进分母；判官对改写偏严的假阴性仍留在指标里，所以支持率只能说"不低于"。

两处的共同点：**判定器与评估口径谁错了，以三态定义为裁判，不以哪边省事为准。**

## 六、评估怎么保持诚实

- **隔离**：两个脚本都只用 `sample-knowledge/` 与 `eval/` 的黄金集，在纯 ASCII 临时目录建隔离索引，只 import 各模块 `public`，不读业务 MySQL、不开业务 Chroma、不写历史、不改模型配置。
- **可对照的前提**：同一题集、同一策略、同一模型才能前后比；模型输出跨运行有波动，支持率与 PARTIAL 计数看总量趋势，逐题翻转（Q17、P3）记为噪声而不是结论。
- **延迟只在可归因的段落下结论**：检索段的毫秒数由代码决定，可比；生成段受第三方接口当日波动影响（曾出现单次 191 s、139 s），需要同时段 A/B 才能比，本轮不写百分比。
- **报告记录样本数字，不当概率保证**（D-4）；报告与同名 JSON（含候选与完整答案）入库 `eval/`，命令行只记相对路径。
- **脚本自己节流**（D-5）：首跑 42 题里 35 题在判定阶段 80 ms 内被第三方接口拒绝，是逐句核对的密集调用触发限流；评估脚本加停顿、冷却和整题重跑，产品代码的"不自动重试"不变。
- **没测的**：真实用户库上的召回（语料同质，v3 的真实问法只有 10 条）；判官自身的准确率；评估看板与自动调参未做，也不打算做。

## 七、复现

```powershell
cd rag-service
# 离线召回：hit@1/3/5/10，本地 embedding，不调回答模型
.\.venv\Scripts\python.exe -m scripts.eval_retrieval --golden-set ../docs/eval/golden-set-v3-hard.md --output ../docs/eval/retrieval-v3.md
# 在线评估：拒答正确率、错源率、引用支持率、首字与完整耗时（按 .env 或 config/llm.json 计费）
.\.venv\Scripts\python.exe -m scripts.eval_answers --faithfulness --output ../docs/eval/answers.md
# 真实失败问法导出（只读，输出在被 Git 忽略的 data/ 下）
.\.venv\Scripts\python.exe -m scripts.export_history_failures
```

| 轮次 | 报告 |
|---|---|
| 离线基线 v1（2026-09-07） | `eval/retrieval-baseline-v1.md` |
| 在线基线（dense，2026-09-21） | `eval/answers-baseline-2026-09-21.md` |
| 混合检索 | `eval/retrieval-v1-hybrid-2026-09-22.md`、`eval/retrieval-v2-{dense,hybrid}-2026-09-22.md`、`eval/answers-hybrid-2026-09-22.md` |
| 证据压缩 | `eval/answers-relevant-2026-09-22.md` |
| 难例集 v3 | `eval/golden-set-v3-hard.md`、`eval/retrieval-v3-{dense,bm25,hybrid}-2026-09-22.md`、`eval/answers-v3-hybrid-2026-09-22.md` |
